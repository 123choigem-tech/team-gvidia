# 제출 폴더는 git에 올리지 않음 (별도 zip으로 제출)
submit/

# API 키
.streamlit/secrets.toml

# 대용량 데이터 (SST 등)
data/raw/
*.nc
*.nc.part

# 로컬 설정 / 캐시 / OS 부산물
.claude/
__pycache__/
*.pyc
.DS_Store

# 환경변수 / 비밀키 (SST API 키 등 — 절대 커밋 금지)
.env
*.env

# 로그
*.log

# 대용량 입력자료 (미공개 관측 원본 → 외부 업로드 금지 / 재취득 가능)
data/khoa_sst/
data/lss/

# 생성 산출물 (파이프라인으로 재생성 가능: NC·이미지·CSV 등)
data/sst/
data/results/

# 자동 재다운로드 가능한 참조자료 (Natural Earth 해안선)
data/naturalearth/
