# PROCESS_LOG 작업 기록

## 작성자 정보
- 팀명: gvidia
- 본인 이름: 김지니(jiny)
- 공통과제: 뉴스 기사에서 고수온 관련 지역을 추출하고, 관심 해역 SST 데이터를 수집·분석·보고하는 반복 업무 자동화
- 개인 역할: 수집 자동화, Home/분석 UI 정리, 보고서 및 제출 로그 정리

## 효과 측정
| 지표 | Before | After |
|------|--------|-------|
| 데이터 확인 | 뉴스 CSV와 지역별 SST 파일을 사람이 각각 열어 건수와 기간을 확인 | Home 화면에서 2025.7~2025.8 기준 건수와 SST 수집 현황 확인 |
| UI 수정 | Streamlit 기본 빨간 CTA와 알림 색상이 업무 화면과 어울리지 않음 | 버튼과 알림을 청록/블루 계열로 통일 |
| 제출 기록 | 작업 중 로그가 깨져 채점자가 읽기 어려움 | UTF-8 한국어 Markdown으로 재작성 |

## 사용 기법
- [x] (a) 서브에이전트 / 역할 분담
- [x] (b) 도구·데이터 연동
- [x] (c) 재사용 산출물

---
### [#1] 데이터 수집·지오코딩 서브에이전트 구성
- 작성자(팀원): 김지니
- 목표: 기사 기반 관심 지역 추출, 좌표 변환, 해양 SST 수집 과정을 역할별로 분리
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "외부 API 데이터 수집 담당 서브에이전트입니다"
  > "일단 수집-적재 자동화 스크립트부터 서브에이전트가 작업해줘"
- 사용한 기법(있으면): a 서브에이전트 / b 도구연동 / c 재사용산출물
- 결과: 지역 지오코딩과 해양 데이터 수집 역할을 분리하고, 수집 결과를 CSV로 저장하는 흐름을 만들었다.
- 막힘 → 해결(있었다면): 외부 API 접근이 항상 보장되지 않아 fallback 데이터 생성 로직을 함께 설계했다.
---

### [#2] SST 수집 모듈 작성
- 작성자(팀원): 김지니
- 목표: Streamlit 앱에서 직접 호출 가능한 SST 수집 함수 작성
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "src/ocean_api.py 파일을 생성해줘"
- 사용한 기법(있으면): b 도구연동
- 결과: `collect_ocean_data()`와 수집 스크립트 생성 흐름을 만들고, 지역별 CSV 저장 구조를 정리했다.
- 막힘 → 해결(있었다면): API 연결 실패 가능성에 대비해 재현 가능한 샘플 SST를 생성하도록 했다.
---

### [#3] Streamlit UI redesign
- 작성자(팀원): 김지니
- 목표: 초안 화면의 촌스러운 느낌을 줄이고 전체 페이지의 디자인 언어 통일
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "초안은 만들었는데 디자인이 안 예뻐 수정 좀 해줘"
- 사용한 기법(있으면): c 재사용산출물
- 결과: `utils/style.py`를 공통 테마로 정리하고 Home, 재난지역, Heat Analysis, Report 페이지에 통일된 오션 스타일을 적용했다.
- 막힘 → 해결(있었다면): Streamlit 기본 컴포넌트 스타일이 강하게 남아 있어 CSS 선택자를 보강했다.
---

### [#4] Report Agent 연결
- 작성자(팀원): 김지니
- 목표: 분석 결과를 보고서 파일로 생성하는 페이지 연결
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "Report 페이지 보고서 에이전트 연결"
- 사용한 기법(있으면): a 서브에이전트 / b 도구연동
- 결과: `agents/report_agent.py`와 `pages/4_Report.py`를 연결해 보고서 생성 흐름을 구성했다.
- 막힘 → 해결(있었다면): 이미지와 CSV 결과 경로가 여러 폴더로 나뉘어 있어 경로 상수를 정리했다.
---

### [#5] Home 기사 수 기준 통일
- 작성자(팀원): 김지니
- 목표: Home 화면의 뉴스 건수를 실제 분석 기간 기준으로 맞추기
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "지금 Home 에 실제 우리 데이터가 아닌거 같은데 310 건 아닌데 기간도 다르고 이거 수정해줘"
- 사용한 기법(있으면): b 도구연동
- 결과: `pages/1_Home.py`에서 뉴스 수를 `2025-07-01 ~ 2025-08-31` 기준으로 필터링해 표시하도록 수정했다.
- 막힘 → 해결(있었다면): 전체 CSV 310건과 발표 기준 기간 수치가 달라 기간 필터를 명시했다.
---

### [#6] Home 기본 분석 기간 수정
- 작성자(팀원): 김지니
- 목표: Home의 기본 기간과 카드 표기를 2025년 7~8월 기준으로 맞추기
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "아니 310 아니 기간 2025.7-8이라니까"
- 사용한 기법(있으면): b 도구연동
- 결과: Home 카드 기간을 `2025.7 ~ 2025.8`로 표기하고, 기본 시작일을 `2025-07-01`로 조정했다.
- 막힘 → 해결(있었다면): 처음 집계 기준을 전체 데이터로 잡았으나 사용자 피드백으로 기간 기준을 정정했다.
---

### [#7] 빨간 버튼 UI 제거
- 작성자(팀원): 김지니
- 목표: 빨간색 버튼과 알림 토스트를 다른 UI로 변경
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "이 버튼들 빨간색 버튼말고 다른 UI 해달라고"
- 사용한 기법(있으면): b 도구연동
- 결과: `utils/style.py`와 `utils/alert_widget.py`를 수정해 CTA 버튼과 알림 토스트를 청록/블루 계열로 바꿨다.
- 막힘 → 해결(있었다면): 일반 버튼과 form submit 버튼의 스타일 대상이 달라 각각 CSS를 추가했다.
---

### [#8] PR 브랜치에서 submit 산출물 제외
- 작성자(팀원): 김지니
- 목표: 제출용 파일은 GitHub PR에 올리지 않고 로컬 제출 폴더에만 유지
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "submit 을 올리지마"
  > "PR 브랜치에서 submit/ 변경을 빼기 다시 push merge 재시도"
- 사용한 기법(있으면): b 도구연동
- 결과: 원격 PR 브랜치에서 제출 로그 파일 변경분을 제거하는 커밋을 만들고 다시 push했다.
- 막힘 → 해결(있었다면): 로컬 작업트리에 다른 변경이 많아 별도 worktree로 PR 브랜치만 수정했다.
---

### [#9] 제출 Markdown 인코딩 복구
- 작성자(팀원): 김지니
- 목표: 깨진 Markdown 제출 파일을 다시 읽을 수 있게 복구
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "지금 md 파일 인코딩깨져서 수정해줘"
- 사용한 기법(있으면): b 도구연동
- 결과: 제출 로그, 체크리스트, assets/evidence README를 UTF-8 한국어 문서로 재작성했다.
- 막힘 → 해결(있었다면): 깨진 문자의 완전한 역변환은 어려워 실제 작업 맥락과 남아 있는 커밋/프롬프트 정보를 기준으로 재구성했다.
---

### [#10] Pipeline 기능 Home 통합
- 작성자(팀원): 김지니
- 목표: 별도 Pipeline 페이지의 실행 로직과 SST 시계열 페이지를 Home으로 옮기고 페이지 수를 줄이기
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "지금 pipeline 에 있는 로직 HOme 으로 이동, pipeline 페이지 삭제, ssttimeseries삭제"
- 사용한 기법(있으면): b 도구연동
- 결과: `pages/1_Home.py`에 파이프라인 실행 흐름과 SST 시계열 섹션을 통합하고, `pages/5_Pipeline.py`, `pages/5_SST_Timeseries.py`를 삭제했다.
- 막힘 → 해결(있었다면): Home에 기능을 옮기면서 기존 화면 흐름이 과해지지 않도록 섹션 단위로 합쳤다.
---

### [#11] SST 시계열 오류 제거
- 작성자(팀원): 김지니
- 목표: Home의 SST 시계열 영역에서 `matplotlib` 미설치 오류가 보이지 않게 수정
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "이 부분 제거해"
- 사용한 기법(있으면): b 도구연동
- 결과: `pages/1_Home.py`의 SST 시계열 그래프를 `matplotlib` 기반에서 `plotly` 기반으로 바꿔 의존성 오류를 제거했다.
- 막힘 → 해결(있었다면): 실행 환경에 `matplotlib`가 없어 에러 블록이 노출되어, 이미 프로젝트에서 쓰는 `plotly`로 대체했다.
---

### [#12] Home 상단 요약 카드 제거
- 작성자(팀원): 김지니
- 목표: Home 상단의 `현재 분석 현황` 카드 영역을 제거하고 화면을 더 단순하게 만들기
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "이 부분 ㅜㅜㅜ"
- 사용한 기법(있으면): b 도구연동
- 결과: `pages/1_Home.py`의 상단 요약 카드 블록을 삭제해 `분석 데이터 업데이트` 영역이 바로 보이도록 정리했다.
- 막힘 → 해결(있었다면): 요약 카드를 지워도 이후 업데이트와 SST 시계열 기능은 유지되도록 데이터 로딩 함수는 남겼다.
---

### [#13] Home 실제 크롤링 출처 반영
- 작성자(팀원): 김지니
- 목표: Home이 정적 CSV나 시드처럼 보이지 않고 실제 크롤링 DB의 출처와 수집 시점을 반영하게 하기
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "아니 지금 Home 에 실제 크롤링이 아닌거 같은데"
- 사용한 기법(있으면): b 도구연동
- 결과: `pages/1_Home.py`가 `EventStore`를 직접 조회해 `origin`, `crawled_at`, `event_date` 기준으로 실제 수집 출처와 기사 기간을 사용하도록 수정했다.
- 막힘 → 해결(있었다면): CSV에는 출처/수집 시점 정보가 부족해 SQLite 저장소를 직접 읽는 방식으로 변경했다.
---
