# PROCESS_LOG — 작업 기록 (과정 70점의 핵심 근거)

> 표준 헤더(CLAUDE.md 등)를 로드했다면 에이전트가 알아서 채워 줍니다. 비면 직접 채우세요.
> 원칙: **실제로 시킨 프롬프트를 그대로 인용**할 것. 요약만 있으면 점수가 깎입니다.

## 작성자 정보 (개인별 로그 — 본인 것만)
- 팀명: Gvidia
- 본인 이름(작성자): choi (최) <!-- 한글 성함 확인 요망 -->
- 공통과제(우리 팀이 자동화한 반복 수작업): 재난(고수온) 뉴스를 모아 발생일·발생지역을 정리하고, 빈도 높은 지역의 해수면온도(SST) 격자자료를 받아 전처리·고수온 분석·가시화하던 일련의 수작업을 웹 플랫폼으로 자동화
- 내가 맡은 부분: 전체 플랫폼 (1단계 = 뉴스 크롤링 → 관심지역(AOI) 도출 웹앱 / 이후 = SST 수집·전처리·고수온/지속성 분석)
- 자유과제(있으면): -

> **이 로그는 본인 것만 작성**합니다. 각자 자기 PC·계정으로 작업해 개인 로그를 남기고, 제출 시 **영문 파일명** `<팀영문명>_<이름로마자>_PROCESS_LOG.md`(예: `teamA_kim_PROCESS_LOG.md`)로 저장하세요.

## 효과 측정 (Before → After, 결과 ⑥ 채점용 — 형식 자유)

| 지표(자기 업무에 맞게) | Before(기존 수작업) | After(에이전트화) |
|------|------|------|
| 재난기사 위치 정리 | 기사 일일이 읽고 발생지역 수기 정리 | 크롤링 시 연안 시·군 지명사전으로 자동 추출 (라이브 60건 중 37건 자동 위치확정) |
| 빈도 높은 관심지역 도출 | 표 만들어 손으로 집계 | 지역별 빈도 집계 → 상위 AOI 자동 산출 + 지도/그래프 |
| 데이터 누적 | 엑셀 파일 분산 관리 | SQLite 누적 DB + URL 기준 중복제거(재실행해도 신규만 적재) |

## 사용 기법 (권장·가점, 필수 아님)
- [x] (a) 서브에이전트 / 역할 분담
  - 초기 설계: Explore/Plan 서브에이전트로 KOSC SST API·NetCDF 스택 조사 + 3개 영역(수온 파이프라인·뉴스/AOI·웹플랫폼) **병렬 설계**
  - 프로젝트 점검: 3개 서브에이전트로 SST·웹앱·요구사항 충족도를 **병렬 코드리뷰**
  - **GIS 전문 서브에이전트 13종**(agency-agents) 설치·활용
- [x] (b) 외부 도구·데이터 연동
  - **공공데이터포털 odcloud API**(한국언론진흥재단 폭염·기후변화 메타데이터) — 기간검색·지역 개체명
  - **빅카인즈 엑셀 적재**(openpyxl) · **네이버 뉴스 검색 API** · Google News RSS · SQLite
- [x] (c) 재사용 산출물
  - 모듈형 `src/news/` 패키지(소스 폴백체인·빅카인즈 로더·연안 지명사전·피해필터)
  - 증빙 자동화 `scripts/log_evidence.py` · 팀페이지 연동 `export_team_csv` 브리지
  - **`geosr-hwpx` 한글(HWPX) 보고서 자동생성 스킬**(설치·활용) + `scripts/build_report.py`
  - **발표자료 자동생성 `scripts/build_presentation.py`**(이미지 내장 단일 HTML)

---

## 작업 로그 (단계마다 1개씩 누적 / 시간순)

### [#1] 뉴스 크롤링 → 관심지역(AOI) 도출 웹앱 (1단계) — 2026-06-30
- 작성자(팀원): choi
- 목표: 재난(고수온) 뉴스를 검색어·기간으로 크롤링해 발생일·발생위치를 누적 DB화하고, 빈도 상위 지역을 관심지역(AOI)으로 자동 도출하는 **Streamlit 웹앱** 구현
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "우선 크롤링부터 구현을 하려고 하는데 뉴스 크롤링에 추천하는 것 있는지? 빅카인즈?"
  > "1. 일단 웹앱 형태로 구현 바람  2. 검색어, 날짜(기간) 선택 가능하게  3. 특정 재난의 발생일, 발생위치를 누적하여 데이터베이스화하고 빈도가 높은 지역을 도출하여 분석하기 위한 관심지역으로 설정(우선 일단위)"
- 사용한 기법: (a) 서브에이전트 조사·설계 / (b) 네이버 API·RSS·SQLite 연동 / (c) 재사용 모듈 패키지
- 결과:
  - 소스 추천: 빅카인즈(위치·날짜 개체명 사전추출, 단 OpenAPI는 승인 필요) vs 네이버 뉴스 검색 API(즉시 발급) 비교 → **즉시 가능한 네이버 API + 키리스 RSS**로 착수, 빅카인즈는 엑셀/키 확보 시 동일 스키마로 연결 예정
  - 웹앱 `app.py` '뉴스 크롤링/관심지역 추출' 페이지: 검색어·발생기간(일단위)·최대건수 입력 → 크롤링 실행 → 이벤트 표 / 지역별 빈도 막대 / 일자별 추이 / 발생지 지도 / AOI 표·직접지정
  - 백엔드 `src/news/`: `sources.py`(네이버 API 우선 → Google News RSS 폴백 → 시드), `parse.py`(날짜+연안 지명사전 위치추출), `store.py`(SQLite 누적·중복제거), `aoi.py`(빈도집계→상위 AOI→`data/aoi.json`)
  - 검증: 시드 30건 → 100% 위치추출 · 라이브 RSS 60건 → 37건 위치추출 · **네이버 API 121건(키워드 2종) → 72건 위치추출, AOI=제주(24)/태안(16)/여수(12)** · 앱 헬스체크 200 OK
  - 네이버 Client ID·Secret `.env` 적용·인증 검증 완료(`source=naver_api`). 키 없을 때도 RSS 폴백으로 실데이터 크롤링 동작 확인
- 막힘 → 해결:
  - 네이버 검색 API는 본문·발생위치를 안 줌 → 제목/요약에서 **연안 36개 시·군 지명사전 + 시도 동시언급 규칙**으로 위치 직접추출(동명이지 '고성' 강원/경남 구분 포함)
  - 빅카인즈 OpenAPI는 신청·승인 필요로 즉시 사용 불가 → 즉시발급 네이버 API로 우회, 빅카인즈는 차순위 보강

### [#2] 공공데이터(한국언론진흥재단) 연동 — 과거 기간검색 + 지역 사전추출 — 2026-06-30
- 작성자(팀원): choi
- 목표: 네이버는 "최근 기사"만 줘서 사용자가 고른 과거 기간이 실제로 검색되지 않던 문제를, 공공데이터포털 한국언론진흥재단 뉴스빅데이터 메타데이터(폭염·기후변화) API로 교체해 **기간(일자) 검색 + 지역 개체명 사전추출**로 해결
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "왜 날짜를 내가 설정했는데 현재기준으로 나오지?"
  > "한국언론진흥재단_뉴스빅데이터_메타데이터_기후변화, …폭염 공공데이터 활용신청 했음 … 이건 내 인증키(decoding)임. 이거로 그럼 바꿔줘 필요한 정보 있음 요청하고"
- 사용한 기법: (b) 외부 API 연동 (data.go.kr odcloud REST), (c) 재사용 소스 모듈
- 결과:
  - 원인규명: 네이버 검색 API엔 날짜 파라미터가 없어 항상 최신순 반환 → 기간선택이 사후필터로만 작동했음
  - odcloud API 연동: 폭염(ID 15153440)·기후변화(15153229) 데이터셋, `cond[일자::GTE/LTE]` 기간검색 + `cond[제목::LIKE]` 키워드 필터, 인증키 `.env(PUBLIC_DATA_API_KEY)`
  - 사전추출 `개체명(지역)`을 위치추출에 우선 활용 → **위치추출율 60%(네이버)→85%(공공데이터)**
  - 검증: 2024-06-01~09-30·고수온 → 291건 수집(전부 신규), 발생일 2024-06-10~09-30(요청창 밖 0건), **AOI 통영(41)·제주(30)·태안(28)**
- 막힘 → 해결:
  - **중복제거 버그**: 공공데이터 기사 URL이 `?newsId=`로만 구분되는데 기존 canonical_url이 쿼리를 통째로 잘라 전부 동일 취급 → urllib 기반으로 추적파라미터(utm_)만 제거하고 식별쿼리는 유지하도록 수정
  - 발생일 정확도: 보도일(일자) 우선 + 본문 날짜 오인 방지를 위해 날짜 파싱은 제목만 사용

### [#3] 빅카인즈 엑셀 적재 — 2025 등 공공데이터 미커버 기간 보강 — 2026-06-30
- 작성자(팀원): choi
- 목표: 공공데이터(한국언론진흥재단)는 **2024-12-31까지만** 있어 2025가 없음 → 빅카인즈 '엑셀 다운로드'(기간 지정)를 업로드해 DB에 누적
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "25년은 그럼 빅카인즈 엑셀로 데이터베이스 구성해줘"
- 사용한 기법: (b) 파일 데이터 연동, (c) 재사용 로더 모듈
- 결과:
  - `src/news/bigkinds.py` 로더: 빅카인즈 NewsResult 표준 컬럼(제목/일자(YYYYMMDD)/위치/언론사/URL/본문/키워드) 견고 매핑, **위치 개체명 우선 활용**
  - 웹앱 **업로드 UI**(엑셀/CSV) + CLI `--bigkinds` + `ingest_bigkinds()` — 동일 DB·AOI 파이프라인으로 누적
  - 검증(샘플 2025): 12건 100% 위치추출, 일자 2025-07-15~08-22, AOI 통영(3)·완도(2)·거제(1)
  - 부수: 실제 API 키 있을 때 **가짜 시드 자동폴백 차단**(allow_seed=False) + 기존 시드 30건 DB 제거(657→627)
  - 팀 멀티페이지 연동: 팀이 `app.py`를 멀티페이지(`pages/`,`utils/`)로 재구성 → `export_team_csv()` 브리지로 내 파이프라인을 팀 페이지가 읽는 `disaster_events.csv`·`region_frequency.csv`로 자동 내보내기
  - 실데이터 확정(요구사항 1): **2024=공공데이터 API 627건 + 2025=빅카인즈 엑셀(NewsResult, 2767행→고수온 1223건)** = 1850건. 관심지역 제주244·통영178·여수137·거제108·기장79
- 막힘 → 해결: 엑셀 읽기용 openpyxl 설치. 진단 중 발견한 "2025년 가짜 30건"은 시드 자동폴백이 원인 → 차단

### [#4] 피해 기사 필터 + 기간필터 반영(빈도/TOP N/지도) — 2026-06-30
- 작성자(팀원): choi
- 목표: 단순 '고수온' 언급이 아닌 **실제 피해 발생 기사**만 필터 + 팀 페이지 기간필터가 빈도·TOP N·지도에도 적용되게
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "'고수온 피해', '양식장' 등 실제 피해 발생 시의 뉴스를 필터링 하고싶은데 방법 알려줘"
  > "기간 설정해도 밑에 빈도수나 지역 top n 이런거는 적용이 안되네"
- 사용한 기법: (b) 데이터 가공, (c) 재사용 필터/페이지
- 결과:
  - 피해 필터(`DAMAGE_TERMS`+`filter_articles`, CLI `--damage`): 제목/본문에 **피해·폐사·집단폐사·적조** 포함 기사만 (양식·어가·경보 등 광범위어는 거의 모든 기사에 있어 제외). → **2025 1226→781**, 2024 313
  - 관심지역 변화: 정책·감시 기사 빠지며 제주 1→3위, **통영(116)·여수(106)** 상위 = 실제 피해 기준으로 정확
  - 팀 페이지(`pages/2_Disaster_Areas.py`): 빈도·TOP N·지도가 고정 CSV 대신 **기간/키워드 필터된 이벤트에서 재계산**되도록 수정(좌표는 region_frequency 조인) + TOP N 순위 버그 수정
  - 발생 기사만(추가 요청: "방류·보험가입 같은 건 빼고 발생 관련으로만"): ① **제목에 피해/폐사/집단폐사/적조/떼죽음 단어가 있어야 통과**(`any_of_title` — 본문에만 스친 홍보·마케팅 기사 배제) ② 제목에 방류·예방·대응·보험·가입·지원·발령·추진 등 행정·정책어 있으면 제외(`EXCLUDE_TITLE_TERMS`) → **최종 2024 147건 / 2025 163건**(실제 피해 발생 기사)
- 막힘 → 해결: `ingest_bigkinds`에 damage_only 시그니처만 넣고 **필터 호출을 누락** → 2025 미적용(1226 그대로) 발견 후 호출 추가

### [#5] (다음) 국가해양위성센터 해수면온도(SST) 수집·전처리
(요구사항 2~3: API 적재 → EPSG:4326 좌표계 + 0~40도 튐값 제거)

### [#6] 분석결과 → 한글(HWPX) 보고서 자동 생성 (실측 그림 임베드) — 2026-06-30
- 작성자(팀원): choi
- 목표: 뉴스 빈도·SST 분석 산출물을 부서 표준 양식의 한글 보고서 1개로 자동 조립(예시본)
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "한글 보고서 예시로 한번 만들어줘"
- 사용한 기법: (c) 재사용 산출물 — `geosr-hwpx` 스킬(부서 표준 HWPX 빌더), (b) 데이터 연동(산출 CSV/PNG)
- 결과:
  - `scripts/build_report.py`를 **그림 임베드판으로 갱신** → 본보고서 `report_coastal_hightemp.hwpx` 재생성(validate VALID, 약 1.2MB). (잠깐 만든 `_example` 중복본은 본보고서로 일원화하며 삭제)
  - 기존 자리표시 캡션 박스 대신 **실제 산출 그림 6장 임베드**: 7·8월 월평균 SST, 일평균 시계열, 28℃ 최대연속일수, 누적빈도, 고수온·저염분 중첩(7/14). 정사각 지도(990×990)는 28000 HWPUNIT로 축소 삽입
  - 표·수치 **실데이터 검증**: 지역빈도(제주35·통영33·거제32…) = `region_frequency.csv`, SST 7월 25.95℃→8월 27.21℃(+1.26), 일최고 **34.9℃(7/19)** = 시계열 CSV 재계산 → 기존 스크립트의 33.9℃ 오기 교정
  - 구성: 개요+6개 분석섹션+결론, □/ㅇ 머리기호·휴먼명조·파란 배너 등 부서 표준 양식 자동 적용
- 막힘 → 해결: 정사각 지도를 본문폭(46000)으로 넣으면 1장이 한 페이지를 다 차지 → 그림 픽셀비(1:1) 확인 후 width 28000으로 축소

### [#7] 분석 산출 이미지를 웹앱 분석 탭에 연동 — 2026-06-30
- 작성자(팀원): choi
- 목표: 산출된 SST 그림을 `pages/3_Heat_Analysis.py`에 노출(① 28℃ 초과 일별 공간분포 ② 전해역 일평균 시계열). main 브랜치에서 직접 작업
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "…/sst_over28/img 해당 폴더에 있는 이미지들을 고수온 공간분포 탭에 넣어줘 / git main에서 바로 작업해줘"
  > "이거를 일평균 sst 시계열 탭에 넣어줘(3_heat_analysis) 메인에서 작업해줘"
- 사용한 기법: (b) 데이터 연동(산출 PNG → Streamlit)
- 결과:
  - **고수온 공간분포 탭 신설**: `st.tabs([…, "고수온 공간분포", …])` + `load_hot28_images()`(파일명 `…_U YYYYMMDD _HOT28` 파싱) → **62일치(7/1~8/31)** 지도를 `st.select_slider` 날짜 스크럽으로 1장씩 표시(기본 7/19)
  - **추세(일평균 SST 시계열) 탭**: 기존 plotly 차트 아래에 `SST_daily_mean_20250701_20250831.png`(domain-mean·max) `st.image` 추가
  - `py_compile` 통과, 로더 동작 검증(62일·시계열 PNG 존재). 파일 없을 때 `st.info` 폴백 처리
- 막힘 → 해결: 없음 (st.image 폭 인자 버전차 우려 → streamlit 1.58 확인 후 코드베이스 관례인 `use_container_width` 사용)


### [#8] 프로젝트 설명 HTML 발표자료 자동생성 — 2026-06-30
- 작성자(팀원): choi
- 목표: 결과 수치보다 전체 '흐름'을 설명하는 깔끔한 단일 HTML 발표자료(5분) 제작
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "해당 프로젝트를 설명하는 html 발표자료를 만들어줘 디자인은 번잡하지 않게 깔끔하게"
  > "각각의 내용에 알맞은 이미지는 들어가야할듯" / "정량지표 보다는 전체적인 흐름을 설명하기위함"
- 사용한 기법: (c) 재사용 산출물 — `scripts/build_presentation.py`, (b) 데이터·이미지 연동
- 결과:
  - **자체 완결형 단일 HTML**(외부 라이브러리 0, 이미지 base64 내장) 10슬라이드: 문제(조세호 밈 "지역을 모르는데 어떻게 분석해요")→솔루션 흐름→5단계 처리→크롤링 소스(빅카인즈·공공데이터)→관심지역 지도→고수온 공간분포(기간 가변)→해수면온도 현황→웹 캡쳐→마무리("하나의 흐름으로 가장 기초적인 업무를 자동화")
  - 숫자 강조 빼고 **흐름 중심**, 글자 크게, 키보드(←→)·스와이프 네비
  - `scripts/build_presentation.py` 재실행으로 갱신(루트의 캡쳐 png 자동 임베드). SST 일최고 34.9℃(7/19)로 교정
  - `submit/assets/`에 `REPORT_GENERATION.md`(보고서 생성 재사용 자산) 정리
- 막힘 → 해결: Streamlit 동적 렌더로 Chrome 헤드리스 캡쳐가 빈화면 → 사용자 캡쳐 이미지를 루트(`web.png` 등)에 두면 자동 임베드하는 구조로 우회

## 마무리 요약 (1~2줄)
- 가장 효과적이었던 에이전트 활용법: 서브에이전트로 외부 API·기술스택을 병렬 조사·설계한 뒤, 키가 없어도 동작하는 폴백(RSS/시드) 구조로 즉시 데모 가능하게 구현
- 다른 팀이 그대로 따라 하려면 필요한 것: `pip install -r requirements.txt` 후 `streamlit run app.py` (네이버 키는 선택)
